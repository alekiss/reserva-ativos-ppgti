import sala from "./sala.jpg"
import projetor from "./projetor.png";
import computador from "./computador.png";
import notebook from "./notebook.jpg";

export const imagens: Record<string, string> = {
  sala: sala,
  projetor: projetor,
  computador: computador,
  notebook: notebook,
};